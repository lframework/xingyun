<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lframework.xingyun.basedata.mappers.ProductCategoryPropertyValueRelationMapper">

    <resultMap id="ProductCategoryPropertyValueRelationDto" type="com.lframework.xingyun.basedata.dto.product.ProductCategoryPropertyValueRelationDto">
        <id column="id" property="id"/>
        <result column="product_id" property="productId"/>
        <result column="property_id" property="propertyId"/>
        <result column="property_item_id" property="propertyItemId"/>
        <result column="property_column_type" property="propertyColumnType"/>
        <result column="property_name" property="propertyName"/>
        <result column="property_text" property="propertyText"/>
    </resultMap>

    <sql id="ProductCategoryPropertyValueRelationDto_sql">
        SELECT
            pp.id,
            pp.product_id,
            p.id AS property_id,
            t.id AS property_item_id,
            p.column_type AS property_column_type,
            p.name AS property_name,
            IF(p.column_type = 3, pp.property_text, t.name) AS property_text
        FROM base_data_product_category_property_value_relation AS pp
        INNER JOIN base_data_product_category_property_definition AS p ON p.id = pp.property_id
        LEFT JOIN base_data_product_category_property_item AS t ON t.id = pp.property_item_id
    </sql>
    <delete id="setCommonToAppoint">
        DELETE FROM base_data_product_category_property_value_relation WHERE property_id = #{propertyId} AND product_id NOT IN (SELECT id from base_data_product WHERE category_id = #{categoryId})
    </delete>
    <delete id="deleteByPropertyIdAndCategoryId">
        DELETE r FROM base_data_product_category_property_value_relation AS r
        INNER JOIN base_data_product AS p ON p.id = r.product_id
        WHERE r.property_id = #{propertyId}
        AND p.category_id = #{categoryId}
    </delete>
    <select id="getByProductId" resultMap="ProductCategoryPropertyValueRelationDto">
        <include refid="ProductCategoryPropertyValueRelationDto_sql"/>
        WHERE pp.product_id = #{productId}
        ORDER BY p.code, t.code
    </select>
    <select id="getByPropertyId" resultMap="ProductCategoryPropertyValueRelationDto">
        <include refid="ProductCategoryPropertyValueRelationDto_sql"/>
        WHERE pp.property_id = #{propertyId}
        ORDER BY p.code, t.code
    </select>
</mapper>
